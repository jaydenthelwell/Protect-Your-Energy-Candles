<div>
  <div class="d-flex justify-content-between"style="gap: 30px; flex-direction: column; text-align: center;">
    <h1 class="edition-title">Luminous Candles</h1>
    <p>Indulge in the serene scents, thoughtfully curated for relaxation and energy protection. Elevate your surroundings, embracing the enchanting ambiance that our handcrafted <br>candles effortlessly bring to your space.</p>
    <br>
  </div>
  <div>
  <%# <%= image_tag("light_edition.jpeg", alt: "Candles", class:"w-100 main-image") %>
  <div class="container container-candles">
    <div class="row gx-5">
      <% @candles.each do |candle| %>
        <div class="col-md-6 col-lg-3 mb-4">
          <%= link_to(candle) do %>
            <div class="card text-center">
              <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                  <div class="carousel-item active">
                    <% candle.images.each do |image| %>
                      <%= image_tag(image.url, class: "card-img-top carousel-item d-block", alt: "Candle Image") %>
                    <% end %>
                  </div>
                </div>
                <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                  <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                  <span class="carousel-control-next-icon" aria-hidden="true"></span>
                </a>
              </div>
              <div class="card-body">
                <h3 class="card-title"><%= candle.name %></h3>
                <p class="card-title"><%= candle.scent %></p>
                <strong><%= number_to_currency(candle.price, unit: '£') %></strong><br><br>
                <p><%= candle.size %></p>
                <%# <a href="#" class="btn">Quick Shop</a> %>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
<%= render 'shared/footer' %>
